%Script that evolutions the vortex curve and plots it

%Circle making 
iter = 500 + 1; % we need iteration # + 1 so that we don't overlap at the end point
s = linspace(0, 2*pi, iter);
s(end) = [];
ys = @(s) cos(s); 
zs = @(s) sin(s);
C = [zeros(1,iter-1); ys(s); zs(s)];
h = abs(s(2) - s(1)); 
T = Derivative(C,h); 


%First test iteration 

C1 = C; 

C2 = BSI_Evol(C1,s); 
pl
plot3(C2(1,:),C2(2,:),C2(3,:))


